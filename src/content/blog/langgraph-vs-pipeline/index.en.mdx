## LangGraph vs Pipeline

Every AI agent system needs orchestration — the logic that decides what happens when, in what order, and under what conditions. Two dominant patterns have emerged: **linear pipelines** and **graph-based execution**. Each has its place.

## Linear Pipelines

A pipeline is a fixed sequence of operations:

\