## Type-Safe Agent State Management

Agent workflows pass state between nodes. Without type safety, state bugs are hard to catch. Here's a TypeScript pattern.

## The Typed State Object

Define the agent's state as a single interface:

\